# Лабораторная работа №5  
## Облачные базы данных: Amazon RDS, DynamoDB

**Дисциплина:** Cloud Computing / Облачные технологии  
**Тема:** Amazon RDS, Read Replica, DynamoDB  
**Студент:** Пысларь Никита 
**Группа:** IA2304 
**Год:** 2026  

---

## 📌 Описание лабораторной работы

В данной лабораторной работе рассматриваются облачные сервисы Amazon RDS (Relational Database Service) и Amazon DynamoDB.  
Цель работы — освоить развёртывание и настройку реляционной базы данных в AWS, подключение к ней с виртуальной машины EC2, выполнение базовых операций с данными (CRUD), а также изучить концепцию Read Replica и принципы работы NoSQL базы DynamoDB.

---

## 🎯 Цель и основные этапы работы

### Цель работы:
- Ознакомиться с сервисом Amazon RDS и научиться создавать экземпляры реляционных баз данных в AWS.
- Понять концепцию Read Replicas и их назначение.
- Научиться подключаться к базе данных RDS с EC2 и выполнять CRUD-операции.
- (Дополнительно) Ознакомиться с Amazon DynamoDB и NoSQL-подходом к хранению данных.

### Основные этапы:
1. Подготовка среды (VPC, Subnets, Security Groups)
2. Развёртывание Amazon RDS (MySQL)
3. Создание виртуальной машины EC2
4. Подключение к базе данных и выполнение SQL-операций
5. Изучение Read Replica
6. Подключение приложения к базе данных (описание)
7. Дополнительное задание: DynamoDB (теоретически)
8. Выводы

---

## 🛠 Практическая часть

---

### Шаг 1. Подготовка среды (VPC / Subnets / Security Groups)

Была создана виртуальная сеть **project-vpc** с:
- 2 публичными подсетями
- 2 приватными подсетями
- Подсети размещены в разных зонах доступности (Availability Zone)

Также были созданы группы безопасности:

#### 🔹 web-security-group
Разрешённый трафик:
- Inbound:
  - HTTP (порт 80) — от любого источника
  - SSH (порт 22) — от моего IP-адреса (для управления EC2)
- Outbound:
  - MySQL/Aurora (порт 3306) — к группе `db-mysql-security-group`

#### 🔹 db-mysql-security-group
Разрешённый трафик:
- Inbound:
  - MySQL/Aurora (порт 3306) — только от `web-security-group`

<img width="1553" height="433" alt="image" src="https://github.com/user-attachments/assets/495bfe24-5db7-4bd8-a8eb-e649ca8b6bf1" />

<img width="1902" height="842" alt="image" src="https://github.com/user-attachments/assets/1424048a-d048-452e-a3af-ec6facf78d76" />


---

### Шаг 2. Развёртывание Amazon RDS

#### Что такое DB Subnet Group?

**DB Subnet Group** — это группа подсетей, в которых AWS может размещать экземпляры базы данных.  
Она необходима для:
- указания, в каких подсетях будет находиться база данных,
- повышения отказоустойчивости за счёт использования подсетей в разных AZ,
- изоляции базы данных в приватной сети.

Была создана Subnet Group:
- **Имя:** `project-rds-subnet-group`
- Использует 2 приватные подсети из разных AZ в `project-vpc`.


#### Создание RDS (MySQL)

Параметры базы данных:
- Engine: MySQL (8.x)
- Template: Free tier
- Deployment: Single-AZ
- DB identifier: `project-rds-mysql-prod-v2`
- Master username: `admin`
- DB instance class: `db.t3.micro`
- Storage: gp3, 20 GB (autoscaling до 100 GB)
- Public access: No
- VPC: `project-vpc`
- Security Group: `db-mysql-security-group`
- Initial database name: `project_db`
- Backups: Enabled

После создания база данных получила статус **Available**.  
Был скопирован **Endpoint** для подключения.
<img width="1511" height="715" alt="image" src="https://github.com/user-attachments/assets/7ec1a2f5-1c63-49b7-b37c-41ab7a8f34b3" />

<img width="332" height="160" alt="image" src="https://github.com/user-attachments/assets/443d1c61-ca2e-4a4f-9dca-378e27f33592" />


---

### Шаг 3. Создание виртуальной машины EC2

Была создана виртуальная машина **EC2** в публичной подсети `project-vpc` с группой безопасности `web-security-group`.

При инициализации был установлен MySQL/MariaDB клиент:

```bash
#!/bin/bash
dnf update -y
dnf install -y mariadb105
```

После этого выполнено подключение к EC2 по SSH.

<img width="1905" height="723" alt="image" src="https://github.com/user-attachments/assets/c295e663-44e4-4bfb-894d-62ed0c5159ec" />

---

### Шаг 4. Подключение к базе данных и выполнение SQL-операций

#### Подключение к RDS с EC2:

```bash
mysql -h <RDS-ENDPOINT> -u admin -p
```

После ввода пароля выполнено успешное подключение к базе данных.

Так как база `project_db` не была создана автоматически, она была создана вручную:

```sql
CREATE DATABASE project_db;
USE project_db;
```


#### Создание таблиц (связь one-to-many):

```sql
CREATE TABLE categories (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL
);

CREATE TABLE todos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    status VARCHAR(50),
    category_id INT,
    FOREIGN KEY (category_id) REFERENCES categories(id)
);
```

<img width="579" height="522" alt="image" src="https://github.com/user-attachments/assets/c77de2be-9eba-4292-b41e-6946501d3622" />

#### Вставка данных:

```sql
INSERT INTO categories (name) VALUES
('Work'),
('Study'),
('Personal');

INSERT INTO todos (title, status, category_id) VALUES
('Finish lab', 'open', 2),
('Buy groceries', 'done', 3),
('Prepare report', 'open', 1);
```

Проверка:

```sql
SELECT * FROM categories;
SELECT * FROM todos;
```

<img width="451" height="412" alt="image" src="https://github.com/user-attachments/assets/c4c31e6a-e411-4121-bea1-4f92a648f65f" />

#### JOIN-запрос:

```sql
SELECT todos.title, todos.status, categories.name AS category
FROM todos
JOIN categories ON todos.category_id = categories.id;
```

<img width="775" height="489" alt="image" src="https://github.com/user-attachments/assets/64102048-97da-49b0-819e-daec16271283" />
---

### Шаг 5. Read Replica

Была выполнена попытка создания Read Replica для основного экземпляра базы данных.  
Однако AWS выдал сообщение о превышении лимитов ресурсов в рамках Free Tier, и создание дополнительного экземпляра оказалось невозможным.

Тем не менее, был изучен принцип работы Read Replica:

- Read Replica используется для масштабирования операций чтения.
- Реплика синхронизируется с основным экземпляром асинхронно.
- Read Replica предназначена только для операций чтения (SELECT).
- Операции записи (INSERT, UPDATE, DELETE) на реплике запрещены.

#### Ответы на контрольные вопросы:

**Какие данные отображаются на реплике и почему?**  
На реплике отображались бы те же данные, что и на основном экземпляре, так как она получает изменения через механизм репликации.

**Можно ли выполнять запись на Read Replica? Почему?**  
Нет, нельзя, так как Read Replica предназначена только для чтения и используется для разгрузки основного экземпляра.

**Отобразилась бы новая запись после вставки на master?**  
Да, новая запись появилась бы на реплике с возможной задержкой из-за асинхронной репликации.

**Зачем нужны Read Replicas?**  
Read Replicas используются для:
- масштабирования операций чтения,
- снижения нагрузки на основной экземпляр,
- выполнения аналитических и отчётных запросов,
- повышения общей производительности системы.

---

### Шаг 6. Подключение приложения к базе данных

В рамках лабораторной работы было изучено подключение приложения к Amazon RDS.  
Из-за ограничений по времени и ресурсам основной упор был сделан на проверку работы базы данных через SQL-клиент и выполнение CRUD-операций вручную.

В реальном проекте приложение может:
- использовать основной экземпляр для операций записи (CREATE, UPDATE, DELETE),
- использовать Read Replica для операций чтения (READ),
что позволяет повысить производительность и масштабируемость системы.

---

### Шаг 7. Дополнительное задание: DynamoDB (теоретически)

Для хранения данных, например задач (Todos), в DynamoDB можно спроектировать таблицу:

- Table name: `Todos`
- Partition Key: `id` (String)

**Обоснование выбора ключа:**  
`id` однозначно идентифицирует запись и позволяет быстро получать данные по ключу.

#### Сравнение DynamoDB и RDS

**Преимущества DynamoDB:**
- высокая производительность,
- автоматическое масштабирование,
- отсутствие необходимости управлять серверами,
- гибкая схема данных.

**Недостатки:**
- отсутствие JOIN-запросов,
- более сложное проектирование структуры данных,
- ограниченные возможности сложных запросов.

#### Сценарий совместного использования RDS и DynamoDB

- Amazon RDS можно использовать для транзакционных и реляционных данных (пользователи, заказы, связи между таблицами).
- DynamoDB можно использовать для логов, кэша, сессий или быстрых key-value данных.

Совместное использование позволяет получить:
- надёжную реляционную модель (RDS),
- высокую скорость и масштабируемость (DynamoDB).

---

## ✅ Вывод

В ходе лабораторной работы были изучены сервисы Amazon RDS и DynamoDB.  
Была развёрнута реляционная база данных MySQL в AWS, выполнено подключение с EC2 и реализованы базовые операции с данными (создание таблиц, вставка данных, SELECT и JOIN-запросы).  
Также была изучена концепция Read Replica и рассмотрены сценарии её применения.  
Дополнительно были рассмотрены принципы работы NoSQL базы данных DynamoDB и варианты совместного использования RDS и DynamoDB в одном приложении.

---

## 📚 Использованные источники

- AWS Documentation: Amazon RDS  
- AWS Documentation: DynamoDB  
- https://docs.aws.amazon.com/  
- Учебные материалы курса
